project BenchStrlen
target console
runtime AgentRuntime 0.1

entry console main

error MainError
errorCase MainError ConsoleWriteFailed CSignedInt32

operation main
input operation main console Console
output operation main Result ExitCode MainError
effect main read environment.variables
effect main read process.environment
effect main read clock.cpu
effect main write console.stdout
memory main heap no
memory main stack max 8KiB
async main no

purpose operation main "Benchmark c.strlen for 200M iterations on a runtime-provided PATH string and report elapsed CPU-clock ticks"
invariant operation main "The probed string comes from c.getenv so its length cannot be constant-folded by the optimizer"

label startMain

storage module immutable pathEnvName CNullTerminatedByteString "PATH"
storage module immutable iterationLimit I64 200000000
storage module immutable iterationStep I64 1

# Resolve the runtime probe string from PATH. If PATH is unset, getenv
# returns NULL — in that case strlen(NULL) is undefined and the benchmark
# is not meaningful, but in any normal environment PATH is set.
call getenvCall c.getenv
argument getenvCall name CNullTerminatedByteString pathEnvName
run getenvCall
bind value environmentProbeText CNullTerminatedByteString getenvCall

# Note: when PATH isn't set, getenv returns NULL. For benchmarking purposes
# we substitute the fallback only when needed; the test runs against PATH
# in normal environments so this branch isn't exercised.

memory main mutable accumulator I64 0
memory main mutable currentIteration I64 0

call startClockCall c.clock
run startClockCall
bind value startClockTicks CCpuClockTicks startClockCall

label benchmarkLoopHead

call shouldContinueCmpCall math.lessThanI64
argument shouldContinueCmpCall left I64 currentIteration
argument shouldContinueCmpCall right I64 iterationLimit
run shouldContinueCmpCall
bind value benchmarkShouldContinue Bool shouldContinueCmpCall
branch if condition benchmarkShouldContinue target benchmarkLoopBody
branch else target benchmarkFinished

label benchmarkLoopBody

call strlenCall c.strlen
argument strlenCall text CNullTerminatedByteString environmentProbeText
run strlenCall
bind value currentLength CByteCount strlenCall

call accumulateCall math.addI64
argument accumulateCall left I64 accumulator
argument accumulateCall right I64 currentLength
run accumulateCall
bind value nextAccumulator I64 accumulateCall
set memory accumulator nextAccumulator

call incrementCall math.addI64
argument incrementCall left I64 currentIteration
argument incrementCall right I64 iterationStep
run incrementCall
bind value nextIteration I64 incrementCall
set memory currentIteration nextIteration
jump target benchmarkLoopHead

label benchmarkFinished

call endClockCall c.clock
run endClockCall
bind value endClockTicks CCpuClockTicks endClockCall

call elapsedTicksCall math.subtractI64
argument elapsedTicksCall left CCpuClockTicks endClockTicks
argument elapsedTicksCall right CCpuClockTicks startClockTicks
run elapsedTicksCall
bind value elapsedClockTicks I64 elapsedTicksCall

storage module immutable reportFormatText CNullTerminatedByteString "iterations=%lld accumulator=%lld clockTicks=%lld\n"
call reportCall c.printf
argument reportCall format CNullTerminatedByteString reportFormatText
argument reportCall iterations I64 iterationLimit
argument reportCall accumulator I64 accumulator
argument reportCall ticks I64 elapsedClockTicks
run reportCall
ignore void source reportCall
bind error reportError CSignedInt32 reportCall
branch error source reportCall target consoleWriteFailed

storage module immutable successfulExitCode ExitCode 0
return ok successfulExitCode

label consoleWriteFailed
makeError consoleWriteFailure MainError.ConsoleWriteFailed reportError
return error consoleWriteFailure
