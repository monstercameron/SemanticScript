[pytest]
# Scope collection to tests/ so a bare `pytest` from the repo root does not try
# to import the deprecated legacy/ tree (legacy/SemanticScript/tests/* import a
# `SemanticScript` package that no longer exists at the root and break collection).
testpaths = tests
norecursedirs = legacy third_party .git __pycache__ apps examples experiments
