# build.sem — TaskForge/EAV project manifest (README §28.1): the project
# entity (identity + target + entry + version pins). Module source lives
# under src/. build.sem.lock is generated by `semanticscript mod tidy`.

TaskforgeWeb is project
TaskforgeWeb module appTaskforgeWeb
TaskforgeWeb target webServer
TaskforgeWeb entry taskForgeWebServer
TaskforgeWeb languageVersion "1.0"
TaskforgeWeb toolchain "semanticscript"
