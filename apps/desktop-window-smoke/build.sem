# build.sem — TaskForge/EAV project manifest (README §28.1): the project
# entity (identity + target + entry + version pins). Module source lives
# under src/. build.sem.lock is generated by `semanticscript mod tidy`.

DesktopWindowSmoke is project
DesktopWindowSmoke module appDesktopWindowSmoke
DesktopWindowSmoke target console
DesktopWindowSmoke entry main
DesktopWindowSmoke languageVersion "1.0"
DesktopWindowSmoke toolchain "semanticscript"
