# build.sem — TaskForge/EAV project manifest (README §28.1): the project
# entity (identity + target + entry + version pins). Module source lives
# under src/. build.sem.lock is generated by `eavc mod tidy`.

EventStreamSmoke is project
EventStreamSmoke module appEventStreamSmoke
EventStreamSmoke target console
EventStreamSmoke entry main
EventStreamSmoke languageVersion "1.0"
EventStreamSmoke toolchain "eavc"
