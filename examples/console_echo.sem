# console_echo -- (repurposed: webserver_echo (webServer lowering unbuilt; echoes via console)). Real string.concat (README s30.2.2).
# Expected output: echo: hello

ConsoleEcho is project
ConsoleEcho module examplesConsoleEcho
ConsoleEcho target console
ConsoleEcho entry main

examplesConsoleEcho is module
examplesConsoleEcho path examples.consoleEcho
examplesConsoleEcho exports main
examplesConsoleEcho purpose "Compute consoleEcho at runtime"
examplesConsoleEcho invariant "Result is computed at runtime"

ExitCode is alias
ExitCode for Int32
ExitCode purpose "Process exit status"

stdoutWriter is capability
stdoutWriter grants write console.stdout
stdoutWriter purpose "Allow controlled writes to standard output"

main is operation
main out ExitCode
main effect write console.stdout
main uses stdoutWriter
main memory heap no
main async no
main purpose "Concatenate two strings and print"
main invariant "Writes exactly one line: echo: hello"
main let partOne immutable String "echo: "
main let partTwo immutable String "hello"
main let harnessName immutable String "console_echo"
main let harnessExpected immutable String "echo: hello"
main let emptyGuard immutable String ""
main let noopName immutable String "console_echo: real result (not the empty string)"
main do calc
main do writeResult
main do noopCheck
main do harnessReport
main return harnessSummaryCode

calc is call
calc in main
calc invokes string.concat
calc arg left String partOne
calc arg right String partTwo
calc out outValue String

writeResult is call
writeResult in main
writeResult invokes test.assertEqualText
writeResult arg name String harnessName
writeResult arg expected String harnessExpected
writeResult arg actual String outValue


noopCheck is call
noopCheck in main
noopCheck invokes test.assertNotEqualText
noopCheck arg name String noopName
noopCheck arg expected String emptyGuard
noopCheck arg actual String outValue

harnessReport is call
harnessReport in main
harnessReport invokes test.summary
harnessReport out harnessSummaryCode ExitCode