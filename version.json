{
  "name": "semanticscript",
  "version": "0.4.0-beta.1",
  "channel": "beta",
  "contractVersion": "eav-0.3.1",
  "note": "Release version source-of-truth. `contractVersion` is the separate source-compatibility contract reported by `semanticscript version`. A release tag must be `v<version>` (e.g. v0.4.0)."
}
